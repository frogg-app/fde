import type { SidebarCalloutProps } from "@/components/sidebar-callout";

/** Desktop update notifications are rendered by the web implementation. */
export function UpdateToast(_props: SidebarCalloutProps) {
  return null;
}
