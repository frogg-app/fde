# Primary dirty-checkout inventory

Read-only audit against `origin/main` commit `14c208b1`, 2026-09-13. No worktree/index/branch changes; no runtime-state contents or credentials read. This is an inventory for user decisions, not approval to merge or dismiss anything. Earlier reconciliation: `docs/project-status.md` at the target commit.

## Sources

- `/home/frogg/projects/ade`: branch `pairing-service-and-installer-fix`, HEAD `09c72a4581cb44d47f477694bff1c5aabe045656`; mixed staged, unstaged and untracked work.
- `/home/frogg/projects/ade-fix`: branch `main`, HEAD `409da97168c4ac01203874978c994d5a8af52297`; substantial staged changes plus an unstaged Cargo.lock version change. Do not move this attached local main branch as part of inventory.

## Candidate work / decisions not present in current main

| ID  | Source and behavior                                                 | Evidence and decision boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P1  | ade: remote directory browsing in Add Project                       | New `apps/ui/src/add-project-flow/directory-browser.ts` and `.test.ts` are absent from main; `components/add-project-flow.tsx` adds current-folder selection, parent navigation, child listing/filtering, keyboard row behavior and directory listing limit100. `e2e/browser/add-project-flow.spec.ts` adds folder navigation coverage. Main still uses existing suggestion picker/native local browse. Candidate selective port and browser/Windows-path verification.                                                                                                                                                              |
| P2  | ade: Android in-app update prompt                                   | Seven new files under `apps/ui/src/native/updates/`, `utils/semver{,.test}.ts`, `app/_layout.tsx`, sidebar-callout native unification and nine locale `mobileUpdates` keys are absent from main. Polls releases on startup/every6hours; sidebar offers APK/download/release notes. Requires decision on recovery/error presentation and proper device ABI selection. Old implementation assumes arm64 and refuses all unsigned assets, whereas current main intentionally publishes debug-signed `-unsigned` assets pending signing migration. Do not port unchanged.                                                                |
| P3  | ade: bind-address-based LAN trust default and truthful claim status | `packages/server/src/server/access-policy.ts` adds `defaultTrustLanForListen`; network-reachable bind defaults to untrusted, local bind remains trusted. Config/bootstrap/claim gate/trust-lan CLI docs/tests follow that rule. `apps/cli/src/commands/daemon/claim.ts` prefers running identity `lanTrusted` over local persisted guess. Main still uses `DEFAULT_TRUST_LAN` and the older claim probe, so this behavior is not integrated. This changes first-pairing behavior and needs an explicit policy decision plus migration tests. Existing `listen-target.ts` extraction in main is already present; do not duplicate it. |
| P4  | ade: Android release-signing enforcement                            | `scripts/release/build-android-apk.mjs` and test add `--require-signing`/`requireSigningMessage`; `docs/android.md` claims unsigned publication should fail. Main deliberately preserves previously debug-signed installed-app update path: `.github/workflows/release.yml` Android job comments and warning explicitly permit `-unsigned` until signing migration is planned. Decision is signing migration/enforcement, not simply copying guard. Old script also lacks main's brand-aware names, development identity and low-memory option; retain those if resumed.                                                             |
| P5  | ade-fix: CLI external-session import                                | New `apps/cli/src/commands/agent/import.ts` + `.test.ts` absent from main. Wraps existing client importAgent with provider/session id/cwd/name/labels and structured output. Candidate independent CLI feature; current main has underlying client import capability but not this command.                                                                                                                                                                                                                                                                                                                                           |
| P6  | ade-fix: CLI open existing agent                                    | New `apps/cli/src/commands/agent/open.ts` absent from main. Resolves explicit/daemon server id and opens existing `openDesktopWithAgent` deep link. Underlying helper exists in main; CLI command is missing. Candidate selective command plus target/open-failure tests.                                                                                                                                                                                                                                                                                                                                                            |
| P7  | ade-fix: CLI GitHub clone                                           | New `apps/cli/src/commands/clone.ts` absent from main; wraps cloneGithubProject with target dir, full remote vs owner/repo protocol validation and capability gating. Neither current root CLI nor project command registers equivalent clone command. Candidate selective port plus success/failure/capability tests.                                                                                                                                                                                                                                                                                                               |
| P8  | ade-fix: CLI surface redesign / speech placeholder                  | `cli.ts`, agent/daemon registration and help-section deletion promote many agent commands to root and change groups; `commands/speech/index.ts` adds only an empty command group. In particular root `stop` targets agents again, conflicting with current daemon-stop semantics and the user's latest fix. Decide command surface separately; P5-P7 need not bring this rewrite. Empty speech group has no implemented speech behavior.                                                                                                                                                                                             |
| P9  | ade-fix: Companion removal                                          | Deletes client Companion implementation/settings/voice helpers and server Companion modules; strips schemas, RPC permissions, capabilities, persisted config, sidebar/keyboard routes, i18n and docs. Current main retains these features and files. This is an explicit product removal proposal with migration consequences, not missing implementation to merge automatically.                                                                                                                                                                                                                                                    |
| P10 | ade-fix: older CI topology and performance/config reversions        | `.github/workflows/ci.yml` removes shared setup action and sharded UI jobs, combines browser/unit suites, reduces server shards5->3. `directory-suggestions.ts` removes settled-directory cache protection and deterministic collator; `git-process-scheduler.ts` lowers2048/s->64/s; `utils/paseo-config-file.ts` removes contentHash concurrency protection (current main renamed FDE file retains it). Docs Rust plan removes measurements/cancelled-stage findings. Main has newer CI and these safeguards. Recommend presenting as historical reversal proposals; replay would remove maintained fixes.                         |

## Already present or superseded evidence

- ade mobile/new-worktree navigation is already integrated: `apps/ui/src/app/new.tsx`, `screens/workspace/workspace-header-menu.tsx`, and `utils/host-routes.ts` are byte-identical to main. Main `new-agent-routing.ts` contains `NewAgentPlacement`/`resolveNewAgentPlacement`; `new-workspace-screen.tsx` contains initialIsolation. Differences are old Paseo identifiers/fixture port or newer main navigation to host connections settings, not missing mobile feature.
- ade `packages/protocol/src/ssh-transport.test.ts` is byte-identical to main. Generated validator churn is older generation/namespace state, not a separate feature.
- ade top-level stop/update convenience work overlaps current main's more recent daemon lifecycle command surface and recovery fixes. Old dual-use `stop [agent]` design is not current main's design; any desire to restore that is a CLI decision, not a lost daemon-stop fix.
- ade installer progress/copy must be reconciled with current brand-aware, checked restart/update implementation. Its new unclaimed-network copy depends on P3, and short `fde pair` instructions depend on command surface; not a standalone installer fix.
- Both checkouts contain stale package/version/Cargo/native-shell settings, old namespace config helpers and generated Rust/protocol outputs. Main's0.6 namespace and app-only Electron architecture supersede wholesale replay; inactive Tauri/Rust reference changes should be separately reviewed only if desired.
- `.dev/paseo-home` in ade includes staged config and untracked daemon identity/runtime files. These are local state, not code candidates; their contents were deliberately not inspected. Preserve separately and exclude from patches.

## Unimplemented ideas recorded only in ade ROADMAP

These are proposed backlog decisions, not implemented work: Android actual supported-ABI detection (`expo-device`); native one-tap APK installer/FileProvider; Settings updates section; mobile prerelease channel; OTA JS update decision; signing-migration notice; distribution channel decision; iOS/TestFlight. P2 implements only initial polling/callout, not these items. User may keep or dismiss each.

## Verification limits

Compared tracked working contents (including staged changes visible in working tree), untracked source paths, relevant semantic diffs and target-main blobs. No tests run because this is inventory-only; existence of old tests does not establish current correctness. No branch/worktree cleanup or merge was performed. Candidate ports require current build/protocol/i18n adaptation and focused tests after user selection.
